[package]
name = "y-lang-compiler"
version = "0.1.0"
edition = "2021"
description = "Prototype Y-Lang compiler with CPU, PTX, C, and LLVM backends."

[lib]
name = "y_lang"
path = "lib.rs"

[[bin]]
name = "y-lang"
path = "main.rs"

[dependencies]
